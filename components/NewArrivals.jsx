import Link from "next/link";
export default function NewArrivals({comics}){return <section className="arrival-strip"><div className="shell"><div className="arrival-heading"><div><p className="eyebrow">Fresh from the vault</p><h2>New Arrivals</h2></div><Link className="text-link" href="/inventory">View all inventory</Link></div><div className="arrival-grid">{comics.map(c=>{const image=c.images?.[0];return <Link key={c.slug} href={`/inventory/${c.slug}`} className="arrival-card"><div className="arrival-image">{image?<img src={image} alt=""/>:<span>{c.title}</span>}</div><div><strong>{c.title}</strong><span>{c.grade}</span><em>{c.price}</em></div></Link>})}</div></div></section>}
