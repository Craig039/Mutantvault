import inventory from "../../data/inventory.json";import ComicCard from "../../components/ComicCard";export const metadata={title:"Sold Archive"};export default function Page(){const sold=inventory.filter(c=>c.status==="sold");return <section className="section page-top"><div className="shell"><div className="page-heading"><p className="eyebrow">Past offerings</p><h1>Sold Archive</h1><p>A permanent record of comics previously offered through Mutant Vault.</p></div><div className="card-grid">{sold.map(c=><ComicCard key={c.slug} comic={c}/>)}</div></div></section>}
