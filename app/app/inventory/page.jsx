import inventory from "../../data/inventory.json";import InventoryBrowser from "../../components/InventoryBrowser";export const metadata={title:"Available Inventory"};export default function Page(){const available=inventory.filter(c=>c.status==="available");return <section className="section page-top"><div className="shell"><div className="page-heading"><p className="eyebrow">Available comics</p><h1>Current Inventory</h1><p>Search and inspect premium graded X-Men and Marvel comics currently available.</p></div><InventoryBrowser comics={available}/></div></section>}
